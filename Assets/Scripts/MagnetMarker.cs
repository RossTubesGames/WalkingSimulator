using UnityEngine;

// This script simply identifies the object as a magnet.
// Keep the Magnet's tag set to "Pickup" — no tag change needed!
public class MagnetMarker : MonoBehaviour
{
    // Empty marker script
}